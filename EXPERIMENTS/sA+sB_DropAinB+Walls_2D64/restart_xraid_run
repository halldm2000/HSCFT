#!/bin/bash
# To be run in the STARTUP_FILES directory in the data folder
echo -en "\e]0;$(pwd)\a"
command=../../../../CODE/HSCFT/hscft_g5
expt=$(pwd)/Experiment.txt
restart_dir=$(pwd)/..
$command -r $restart_dir -x $expt