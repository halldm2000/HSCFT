#!/bin/bash
#!/bin/bash
echo -en "\e]0;$(pwd)\a"# To be run in the STARTUP_FILES directory in the data folder
command=../../../../CODE/HSCFT/hscft_g5
expt=$(pwd)/Experiment.txt
restart_dir=$(pwd)/..
$command -r $restart_dir -x $expt